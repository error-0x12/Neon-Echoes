# Neon Echoes 依赖库列表
# 最低 Python 版本: 3.8+

# ═══════════════════════════════════════════════════
# 核心依赖 (必需)
# ═══════════════════════════════════════════════════

# 用于 GUI 开发 (制谱器使用)
PySide6>=6.4.0

# 用于音频播放 (跨平台: Windows / Linux / macOS)
pygame>=2.0.0

# 用于音频格式转换 (跨平台，需要系统安装 ffmpeg)
pydub>=0.25.0

# 用于中文显示配置
matplotlib>=3.7.0

# ═══════════════════════════════════════════════════
# 可选依赖 (过去曾使用，当前代码中未直接引用)
# 仅在需要替代输入方案时安装
# ═══════════════════════════════════════════════════

# 用于键盘输入检测 (Windows 支持良好，Linux 需要 root 权限)
# keyboard>=0.13.5

# 用于高级键盘事件监听 (跨平台，Linux 需要 X11)
# pynput>=1.7.0

# ═══════════════════════════════════════════════════
# 平台说明
# ═══════════════════════════════════════════════════
# Windows: 输入使用 msvcrt + Win32 GetAsyncKeyState API
# Linux/macOS: 输入使用 termios + select (标准库，无需额外依赖)
# 音频: 使用 pydub (需 ffmpeg) 进行格式转换
